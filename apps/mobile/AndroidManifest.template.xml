<?xml version="1.0" encoding="utf-8"?>
<!--
  AndroidManifest.xml Template for PagoPy Mobile App

  IMPORTANT: This is a TEMPLATE file for reference only.
  The actual AndroidManifest.xml is auto-generated when you run: npx cap add android
  Location: apps/mobile/android/app/src/main/AndroidManifest.xml

  After running 'npx cap add android', manually add the permissions from this template
  to the generated AndroidManifest.xml file.

  DO NOT replace the auto-generated file entirely - only ADD the required permissions.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        android:requestLegacyExternalStorage="true"
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:exported="true">

            <!-- Main launcher intent -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep linking / App Links -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="pagopy.py" />
                <data android:scheme="https" android:host="app.pagopy.py" />
                <data android:scheme="pagopy" />
            </intent-filter>

            <!-- Handle shared content (images, files) -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
                <data android:mimeType="application/pdf" />
            </intent-filter>

        </activity>

        <!-- File Provider for sharing files (invoices, PDFs) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Firebase Cloud Messaging (if using FCM for push notifications) -->
        <!-- Uncomment if you implement FCM -->
        <!--
        <service
            android:name=".FirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        -->

    </application>

    <!-- ==================== REQUIRED PERMISSIONS ==================== -->

    <!-- INTERNET & NETWORK - Essential for API communication -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <!-- CAMERA - For product photos, barcode scanning, document scanning -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.flash"
        android:required="false" />

    <!-- BLUETOOTH - For thermal printer connectivity -->

    <!-- Legacy Bluetooth (Android 11 and below) -->
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <!-- Modern Bluetooth (Android 12+) -->
    <!-- BLUETOOTH_SCAN - Discover Bluetooth devices -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />

    <!-- BLUETOOTH_CONNECT - Connect to paired Bluetooth devices -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_CONNECT"
        tools:targetApi="s" />

    <!-- BLUETOOTH_ADVERTISE - Advertise to other Bluetooth devices (optional) -->
    <!-- Uncomment if needed -->
    <!--
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADVERTISE"
        tools:targetApi="s" />
    -->

    <!-- Bluetooth hardware features -->
    <uses-feature
        android:name="android.hardware.bluetooth"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="false" />

    <!-- LOCATION - Required for Bluetooth device discovery on Android 12+ -->
    <!-- Note: With 'neverForLocation' flag on BLUETOOTH_SCAN, these may not be needed -->
    <!-- Uncomment if Bluetooth scanning requires location -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- NOTIFICATIONS - For push notifications and local notifications -->
    <!-- Required for Android 13+ (Tiramisu) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- STORAGE - For file operations, invoice PDFs, offline data -->

    <!-- Legacy Storage (Android 12 and below) -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"
        tools:ignore="ScopedStorage" />

    <!-- Modern Granular Media Permissions (Android 13+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- VIBRATION - For haptic feedback -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- WAKE LOCK - Keep device awake during sync operations -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- FOREGROUND SERVICE - For background sync operations -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- RECEIVE_BOOT_COMPLETED - Start services on device boot (optional) -->
    <!-- Uncomment if you need to start services on boot -->
    <!--
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    -->

    <!-- REQUEST_IGNORE_BATTERY_OPTIMIZATIONS - Prevent battery optimization (optional) -->
    <!-- WARNING: Google Play may reject if misused -->
    <!-- Only use if absolutely necessary for critical background operations -->
    <!--
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    -->

    <!-- BIOMETRIC - For fingerprint/face authentication (optional) -->
    <!-- Uncomment if implementing biometric authentication -->
    <!--
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    -->

    <!-- NFC - For NFC payments or NFC card reading (optional) -->
    <!-- Uncomment if implementing NFC features -->
    <!--
    <uses-permission android:name="android.permission.NFC" />
    <uses-feature
        android:name="android.hardware.nfc"
        android:required="false" />
    -->

    <!-- SYSTEM ALERT WINDOW - Show overlays on top of other apps (rarely needed) -->
    <!-- WARNING: Dangerous permission, Google Play scrutiny -->
    <!-- Only use if absolutely necessary -->
    <!--
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    -->

    <!-- REQUEST_INSTALL_PACKAGES - Install APK updates (optional) -->
    <!-- Only if implementing in-app updates via APK download -->
    <!--
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    -->

    <!-- ==================== HARDWARE FEATURES ==================== -->

    <!-- Declare hardware features used by the app -->
    <!-- android:required="false" means app works without it but can use it if available -->

    <!-- Touchscreen -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="true" />

    <!-- WiFi -->
    <uses-feature
        android:name="android.hardware.wifi"
        android:required="false" />

    <!-- Location (GPS) -->
    <uses-feature
        android:name="android.hardware.location"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false" />

    <!-- Screen sizes support -->
    <supports-screens
        android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true"
        android:anyDensity="true" />

    <!-- ==================== QUERIES (Android 11+) ==================== -->
    <!-- Declare intents your app needs to query -->
    <queries>
        <!-- If your app opens web links -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>

        <!-- If your app opens email -->
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>

        <!-- If your app opens phone dialer -->
        <intent>
            <action android:name="android.intent.action.DIAL" />
            <data android:scheme="tel" />
        </intent>

        <!-- If your app opens maps -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="geo" />
        </intent>

        <!-- If your app shares content -->
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="*/*" />
        </intent>
    </queries>

</manifest>
